{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core permissions for the single native OMP Switch window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
