Issue #16 Pencil visual verification

Reference frames: `designs/01-setup-success.png`, `10-overview-normal.png`, `20-providers-list.png`, `30-provider-detail.png`, `50-roles-dirty.png`, `60-settings-normal.png`, `23-provider-create-step1.png`, `24-provider-create-step2.png`, `32-model-create-sheet.png`. Each reference is 1536x1024. Pencil node IDs: `R6EUPs`, `chFfk`, `H6IsaW`, `vz6sg`, `i5xFP`, `W7copJ`.

The earlier local files under `.artifacts/issue-16/ui-acceptance/` were generated from a test-only `decorations:false` configuration and are invalidated as release evidence. They remain only as debugging output and must not be used for issue closure.

Release evidence contract:
- Build the UI evidence binary with the formal `src-tauri/tauri.conf.json`, including native decorations and the native `OMP Switch` title.
- On every matrix platform, establish and assert exactly `window.innerWidth === 1536` and `window.innerHeight === 1024` before capture.
- Validate the raw WebDriver PNG is exactly `1536 * devicePixelRatio` by `1024 * devicePixelRatio`; downsample only that exact DPR image to 1536x1024. No stretching or cropping is allowed.
- Produce `setup-success.png`, `overview.png`, `providers-list.png`, `provider-detail.png`, `roles-dirty.png`, `settings.png`, `provider-create-step1.png`, `provider-create-step2.png`, and `model-create-sheet.png` in each platform artifact `omp-switch-<matrix-id>-ui`.

Platform-by-platform comparison is intentionally pending until the formal-decoration workflow run produces all five required platform artifacts: macOS Intel, macOS Apple Silicon, Windows x64, Ubuntu 22.04 x64, and Ubuntu 24.04 runtime. Issue #16 must remain open until those artifacts are downloaded, dimension-checked, and compared page by page against the Pencil references.
