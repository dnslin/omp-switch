Issue #16 Pencil visual verification

Reference source:
- Persistent Pencil file: `designs/omp-switch.pen` (saved through the Pen editor; `git status` shows the intended modification).
- Full-page exports with inherited page background: `.artifacts/issue-16/pencil-reference-full/{R6EUPs,chFfk,H6IsaW,vz6sg,i5xFP,W7copJ}.png`, each 1536×1024.
- Comparable content references: `.artifacts/issue-16/pencil-reference-content/{setup-success,overview,providers-list,provider-detail,roles-dirty,settings}.png`, each generated from the corresponding full-page export by removing the exact top 64px titlebar band with an RGBA-preserving crop. These are not transparent child-node exports.
- Pencil page nodes: `R6EUPs` Setup Success, `chFfk` Overview, `H6IsaW` Providers List, `vz6sg` Provider Detail, `i5xFP` Roles Dirty, `W7copJ` Settings.
- Each approved page is 1536×1024 with a 64px `Window Title Bar` child and a 1536×960 main/content child. The content nodes are `cprjw`, `fwzhN`, `fIk4S`, `TRT2S`, `K8AfL`, and `MvqC4`; their transparent direct exports are not used as visual baselines.

Content comparison contract:
- Product behavior keeps the Overview test-result fields required by `docs/mvp.md` and `docs/flow.md`: model, effective protocol, final address, latency, HTTP status, and time. The persistent approved `chFfk` node includes effective protocol and final address.
- The Overview sync strip keeps `最近备份 —`; issue #16 does not add backup-time metadata or a new DTO.
- Formal evidence builds use `src-tauri/tauri.conf.json`; no `decorations:false` override and no test-only titlebar overlay.
- `tauri-plugin-wdio-webdriver` captures a WKWebView snapshot. Native OS titlebar/window controls are outside the visual comparison and are verified separately by formal decorated packaged-app smoke and startup configuration.
- Every matrix run now produces both `viewport/` (nine real application PNGs at a fixed 1536×1024 WebView viewport, satisfying issue #16) and `content/` (nine 1536×960 PNGs used for Pencil comparison). `window-geometry.json` records the requested native baseline and actual outer physical rect; it does not claim a uniform final outer height.

WK snapshot extra-band investigation:
- On the local macOS content run at DPR 2, `window.innerWidth/innerHeight` and `visualViewport` were 1536×960, while the endpoint returned a raw 3072×1984 PNG. The extra 64 physical rows are the bottom band after the 3072×1920 content rectangle.
- The raw PNG was retained as `.artifacts/issue-16/ui-acceptance-content-final/raw-snapshot.png`.
- Pixel inspection and the runtime normalization guard found every RGBA pixel in the extra band equal to `[248,249,252,255]`, the page background; no content crosses the boundary. Normalization therefore excludes only this proven, uniform bottom backing-surface band. A non-uniform extra band fails the test rather than being cropped.
- This is an endpoint/backing-surface size discrepancy, not the Pencil 64px titlebar. The code does not use a generic top-left crop or infer native chrome height.

Local final macOS evidence:
- Test binary: `src-tauri/target/debug/omp-switch`, built with formal native decorations and `webdriver` only for the test seam.
- Viewport command: `OMP_SWITCH_UI_BINARY=src-tauri/target/debug/omp-switch OMP_SWITCH_UI_OUTPUT=.artifacts/issue-16/ui-acceptance-viewport-final OMP_SWITCH_UI_HEIGHT=1024 corepack pnpm exec wdio run wdio.ui-acceptance.mjs`.
- Content command: `OMP_SWITCH_UI_BINARY=src-tauri/target/debug/omp-switch OMP_SWITCH_UI_OUTPUT=.artifacts/issue-16/ui-acceptance-content-final OMP_SWITCH_UI_HEIGHT=960 corepack pnpm exec wdio run wdio.ui-acceptance.mjs`.
- Both results: 1 spec passed; nine states captured; viewport PNGs are 1536×1024; content PNGs are 1536×960; raw snapshots retained; bottom-band verification passed; window geometry recorded. macOS menu bar and Dock were hidden for deterministic dimensions and restored after each run.

Formal platform comparison completed from CI run `32367195692` (`dd5b0c9`) after downloading all matrix artifacts to `.artifacts/issue-16/ci-32367195692/`.
- macOS arm64, macOS Intel, Windows x64, Ubuntu 22.04 x64, and Ubuntu 24.04 x64 each produced the nine required real-application viewport states: Setup Success, Overview, Providers List, Provider Detail, Roles Dirty, Settings, Provider creation Step 1, Provider creation Step 2, and Model Create Sheet.
- Every viewport PNG is 1536×1024. Every Pencil-comparison `content/` PNG is 1536×960; retained raw snapshots and normalization records are present. macOS WK snapshots exclude only the proven uniform 28px bottom backing band; Windows and Linux snapshots require no exclusion.
- Compared the CI content states with the approved Pencil content baselines and the local final recheck. No unapproved visible layout, size, alignment, spacing, typography, weight, color, radius, border, shadow, icon, copy, component-state, overlay, dialog, or sheet deviation was found. Runtime-only values (OMP version, native paths, platform/architecture, and test result) are expected data differences.
- The five platform smoke records all report `launched: true`; release bundles and UI/smoke artifacts are retained under the same run directory.
- Visual comparison itself closes with no unapproved visible difference. The separate Windows client-host gate remains open: `windows-2022` is Windows Server 2022, not Windows 10 22H2 or Windows 11 x64; no client host was available in this environment, so this record does not waive that acceptance criterion.