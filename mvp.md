# OMP Switch 第一阶段 MVP 文档

> 版本：v0.1  
> 状态：待确认

## 1. 项目说明

OMP Switch 是一个基于 Tauri 的桌面配置工具，用来管理 OMP 的自定义 Provider、模型和角色配置。

第一阶段只负责配置管理和模型连接测试，不负责启动 OMP，也不处理终端相关操作。

用户完成的主要流程是：

```text
检测 OMP
→ 创建自定义 Provider
→ 添加模型
→ 测试模型
→ 设置模型角色
→ 保存到 OMP 配置
```

---

## 2. MVP 范围

第一阶段包含：

- OMP 环境检测
- 自定义 Provider 管理
- API Key 管理
- 模型管理
- 模型连接测试
- 模型角色管理
- OMP 配置文件读写
- 配置备份和冲突检测
- 应用设置
- 基础桌面界面
- 错误提示和日志
- 单元测试和基础界面测试

第一阶段明确不包含：

- 启动 OMP
- 打开或管理终端
- 工作目录选择
- 会话管理
- Provider 预设
- OAuth 登录
- 环境变量形式的 API Key
- `!command` 形式的 API Key
- 模型自动发现
- Skills 管理
- 项目级角色配置
- 自动更新
- 数据库
- 在线 Provider 或模型市场

---

## 3. 技术栈

| 用途 | 技术 |
|---|---|
| 桌面框架 | Tauri 2 |
| 后端 | Rust |
| 前端 | React |
| 开发语言 | TypeScript |
| 构建工具 | Vite |
| UI 组件 | shadcn/ui |
| 样式 | Tailwind CSS |
| 全局状态 | Zustand |
| 表单 | React Hook Form |
| 数据校验 | Zod |
| 表单校验适配 | `@hookform/resolvers` |
| 图标 | lucide-react |
| 动画 | Motion，使用 `motion/react` |
| 消息提示 | Sonner |
| 前端测试 | Vitest + Testing Library |
| Rust 测试 | Cargo test |
| 包管理器 | pnpm |

TypeScript 开启严格模式。

第一阶段不引入数据库。Provider、模型和角色数据直接来自 OMP 配置文件。

---

## 4. 页面结构

第一阶段包含以下页面：

```text
首次检查
概览
Providers
Provider 详情
角色
设置
```

### 4.1 首次检查页面

当应用第一次启动，或者找不到 OMP 时显示。

页面包含：

- OMP 检测状态
- OMP 可执行文件路径
- OMP 版本
- OMP 配置目录
- 配置目录读写状态
- 自动检测按钮
- 手动选择 OMP 按钮
- 重新检测按钮
- 打开配置目录按钮

### 4.2 概览页面

概览页用于查看当前配置状态和快速测试模型。

页面包含：

- OMP 安装状态
- OMP 版本
- Provider 数量
- 模型数量
- 已配置角色数量
- 当前选择的 Provider
- 当前选择的模型
- 当前模型的协议
- 上下文窗口
- 是否支持推理
- 快速测试模型按钮
- 配置文件状态
- 最近一次模型测试结果

概览页不提供启动 OMP 的按钮。

### 4.3 Providers 页面

页面包含：

- Provider 列表
- Provider 搜索
- Provider 状态
- Provider 下的模型数量
- 新增 Provider
- 编辑 Provider
- 删除 Provider
- 空状态提示

### 4.4 Provider 详情页面

Provider 配置和模型管理放在同一个详情页面。

页面包含：

- Provider 基础信息
- API Key 状态
- Provider 保存操作
- Provider 删除操作
- 模型列表
- 模型搜索
- 新增模型
- 编辑模型
- 复制模型
- 删除模型
- 模型测试

### 4.5 角色页面

页面包含：

- OMP 内置角色列表
- 每个角色对应的模型
- Thinking Level 设置
- 无效模型提示
- 保存角色配置
- 清除单个角色
- 清除全部角色

### 4.6 设置页面

页面包含：

- OMP 可执行文件路径
- 重新检测 OMP
- 恢复使用系统 PATH
- 主题设置
- 打开 OMP 配置目录
- 打开应用配置目录
- 打开应用日志目录
- 应用版本
- OMP 版本
- 恢复应用默认设置

---

## 5. OMP 环境检测

应用启动后自动检测 OMP。

### 功能清单

- [ ] 从系统 `PATH` 查找 `omp`
- [ ] 支持用户手动选择 OMP 可执行文件
- [ ] 保存用户选择的 OMP 路径
- [ ] 执行 `omp --version` 获取版本
- [ ] 执行 `omp config path` 获取配置目录
- [ ] 检查 OMP 文件是否存在
- [ ] 检查 OMP 文件是否可以执行
- [ ] 检查配置目录是否存在
- [ ] 检查配置目录是否可读
- [ ] 检查配置目录是否可写
- [ ] 检查 `models.yml` 是否存在
- [ ] 检查 `config.yml` 是否存在
- [ ] 配置文件不存在时可以创建基础文件
- [ ] 配置文件格式错误时显示错误位置
- [ ] 检查 OMP 版本是否低于最低支持版本
- [ ] 支持手动重新检测
- [ ] 支持打开 OMP 配置目录
- [ ] OMP 路径失效时提示用户重新选择

OMP 环境检测只执行必要的状态查询，不启动 OMP 的交互界面。

---

## 6. 自定义 Provider 管理

第一阶段只支持自定义 Provider，不提供任何 Provider 模板。

### Provider 字段

```ts
type ProviderForm = {
  id: string
  baseUrl: string
  api:
    | "openai-completions"
    | "openai-responses"
    | "anthropic-messages"
  authMode: "api-key" | "none"
  apiKey?: string
}
```

### 功能清单

- [ ] 显示已有 Provider
- [ ] 新增 Provider
- [ ] 编辑 Provider
- [ ] 删除 Provider
- [ ] 搜索 Provider
- [ ] Provider ID 不能为空
- [ ] Provider ID 不能重复
- [ ] Provider ID 需要符合基本格式要求
- [ ] Base URL 不能为空
- [ ] Base URL 需要是有效地址
- [ ] 选择 API 协议
- [ ] 选择是否需要 API Key
- [ ] 输入直接 API Key
- [ ] 支持不需要认证的本地服务
- [ ] 保存成功后显示提示
- [ ] 保存失败后显示具体原因
- [ ] 删除前进行二次确认
- [ ] 删除前显示 Provider 下的模型数量
- [ ] 删除前显示受影响的角色
- [ ] 从已有 `models.yml` 读取 Provider
- [ ] 保留应用不认识的 Provider 字段

Provider ID 修改后，需要同步更新相关模型和角色引用。

---

## 7. API Key 管理

第一阶段只支持用户直接输入 API Key。

API Key 按照 OMP 的配置方式保存到 OMP 配置文件中，应用不额外保存一份。

### 功能清单

- [ ] 新建 Provider 时可以填写 API Key
- [ ] 编辑 Provider 时不把旧 API Key 返回到前端
- [ ] 前端只接收 `hasApiKey` 状态
- [ ] 编辑时 API Key 输入框默认留空
- [ ] 输入框留空表示保留原来的 API Key
- [ ] 支持替换 API Key
- [ ] 支持删除 API Key
- [ ] 删除 API Key 时进行确认
- [ ] API Key 不保存到 Zustand
- [ ] API Key 不保存到 localStorage
- [ ] API Key 不保存到应用自己的配置文件
- [ ] API Key 不出现在前端日志
- [ ] API Key 不出现在 Rust 日志
- [ ] 网络错误信息自动隐藏密钥
- [ ] 请求头信息自动脱敏
- [ ] 模型测试完成后不保留完整请求内容

前端提交 API Key 后，应尽快清空对应表单状态。

---

## 8. API 协议支持

第一阶段支持三种协议：

- [ ] `openai-completions`
- [ ] `openai-responses`
- [ ] `anthropic-messages`

每种协议需要分别处理：

- 请求地址
- 请求头
- API Key
- 最小测试请求
- 响应结果
- 错误信息

### 基本规则

- OpenAI 兼容协议使用 Bearer Token
- Anthropic 协议使用对应的 API Key 请求头
- 当认证方式为 `none` 时，不添加 API Key
- 不支持的协议不能执行模型测试
- 不支持的协议需要显示清楚原因
- 每种协议的测试逻辑在 Rust 中分别实现
- 前端不直接拼接模型测试请求

---

## 9. 模型管理

模型属于某个 Provider。

### 模型字段

```ts
type ModelForm = {
  id: string
  name: string
  reasoning: boolean
  input: Array<"text" | "image">
  contextWindow: number
  maxTokens: number
}
```

第一阶段模型默认使用 Provider 的 API 协议，不支持模型单独覆盖协议。

### 功能清单

- [ ] 显示 Provider 下的模型
- [ ] 搜索模型
- [ ] 按模型 ID 排序
- [ ] 新增模型
- [ ] 编辑模型
- [ ] 复制模型
- [ ] 删除模型
- [ ] 显示完整模型标识 `provider/model`
- [ ] 模型 ID 不能为空
- [ ] 同一个 Provider 下模型 ID 不能重复
- [ ] 模型名称不能为空
- [ ] 上下文窗口必须是有效正整数
- [ ] 最大输出 Token 必须是有效正整数
- [ ] 最大输出 Token 不能明显超过上下文窗口
- [ ] 设置模型是否支持 reasoning
- [ ] 设置模型是否支持文本输入
- [ ] 设置模型是否支持图片输入
- [ ] 删除模型前进行二次确认
- [ ] 删除前显示受影响的角色
- [ ] 从已有 `models.yml` 读取模型
- [ ] 保留应用不认识的模型字段
- [ ] Provider 没有模型时显示空状态
- [ ] 删除 Provider 时同时处理其模型
- [ ] 修改模型 ID 时同步更新角色引用
- [ ] 模型表单有修改但未保存时显示提示
- [ ] 离开未保存的模型表单时进行确认

模型复制后自动生成一个不冲突的临时 ID，用户保存前仍可以修改。

---

## 10. 模型角色管理

第一阶段支持以下十个 OMP 内置角色：

```text
default
smol
slow
vision
plan
designer
commit
tiny
task
advisor
```

### 功能清单

- [ ] 显示全部十个内置角色
- [ ] 为每个角色选择 Provider 和模型
- [ ] 为每个角色设置 Thinking Level
- [ ] 保存角色配置
- [ ] 清除单个角色配置
- [ ] 一键清除全部内置角色配置
- [ ] 一键把当前模型设置为 `default`
- [ ] 从 `config.yml` 读取已有角色配置
- [ ] 模型不存在时显示无效状态
- [ ] Provider 不存在时显示无效状态
- [ ] Provider 删除前显示受影响角色
- [ ] 模型删除前显示受影响角色
- [ ] Provider ID 修改后更新角色引用
- [ ] 模型 ID 修改后更新角色引用
- [ ] 保留应用不认识的自定义角色
- [ ] 页面存在未保存修改时显示状态
- [ ] 离开未保存页面时进行确认

Thinking Level 的可选值以当前 OMP 支持的配置为准，不在前端写死无法扩展的规则。

---

## 11. 模型连接测试

模型测试由 Rust 发起，前端只提交 Provider 和模型标识。

### 功能清单

- [ ] 在 Provider 详情页测试模型
- [ ] 在概览页快速测试模型
- [ ] 根据协议生成最小请求
- [ ] 显示测试中状态
- [ ] 防止重复点击测试
- [ ] 支持设置请求超时
- [ ] 支持用户取消测试
- [ ] 显示测试成功或失败
- [ ] 显示请求耗时
- [ ] 显示 HTTP 状态码
- [ ] 显示简单的结果说明
- [ ] 页面内保留最近一次测试结果
- [ ] 使用 Sonner 显示测试结果提示
- [ ] 测试完成后不保存完整响应正文
- [ ] API Key 不返回前端
- [ ] 日志中的请求内容自动脱敏

### 错误提示

需要区分以下情况：

- Base URL 格式错误
- DNS 解析失败
- 无法连接服务
- TLS 或证书错误
- 请求超时
- 用户取消请求
- API Key 错误
- 没有访问权限
- 接口不存在
- 模型不存在
- 请求过于频繁
- Provider 服务异常
- 返回内容格式不兼容

错误信息需要尽量告诉用户问题出现在哪一层，避免只显示“请求失败”。

---

## 12. 配置文件管理

Provider 和模型读取自 `models.yml`，角色读取自 `config.yml`。

所有配置文件操作由 Rust 完成，前端不能直接读取或修改任意文件。

### 读取配置

- [ ] 读取 `models.yml`
- [ ] 读取 `config.yml`
- [ ] 解析 Provider
- [ ] 解析模型
- [ ] 解析角色
- [ ] 发现格式错误时停止编辑
- [ ] 显示配置错误所在位置
- [ ] 保留应用不认识的字段

### 保存配置

保存配置时按以下顺序处理：

```text
读取最新文件
→ 检查文件是否被外部修改
→ 创建备份
→ 修改目标内容
→ 检查新配置是否有效
→ 写入临时文件
→ 替换原文件
```

### 功能清单

- [ ] 保存前重新检查配置文件
- [ ] 保存前检查文件是否被其他程序修改
- [ ] 修改 Provider 时只更新对应 Provider
- [ ] 修改模型时只更新对应模型
- [ ] 修改角色时只更新对应角色
- [ ] 保存前检查完整配置是否可以重新解析
- [ ] 使用临时文件完成写入
- [ ] 确认临时文件有效后再替换原文件
- [ ] 写入失败时保留原文件
- [ ] 配置被外部修改时拒绝直接覆盖
- [ ] 提供重新加载配置按钮
- [ ] 配置有错误时禁止继续写入
- [ ] 文件路径全部由 Rust 管理
- [ ] 前端不能传入任意目标路径进行写入

应用需要保留未知配置字段，但不保证完全保留原 YAML 的空行、缩进和注释格式。

---

## 13. 配置备份

每次修改 OMP 配置前自动创建备份。

### 功能清单

- [ ] 每次写入前创建备份
- [ ] `models.yml` 和 `config.yml` 分别备份
- [ ] 备份文件名包含日期和时间
- [ ] 自动保留最近 10 份备份
- [ ] 超出数量后删除最旧备份
- [ ] 备份失败时中止正式写入
- [ ] 备份文件保存在应用管理的备份目录

第一阶段只做自动备份，不提供备份列表和手动恢复页面。

---

## 14. 应用设置

### 功能清单

- [ ] 设置自定义 OMP 可执行文件路径
- [ ] 恢复使用系统 `PATH` 中的 OMP
- [ ] 浅色主题
- [ ] 深色主题
- [ ] 跟随系统主题
- [ ] 打开 OMP 配置目录
- [ ] 打开应用配置目录
- [ ] 打开应用日志目录
- [ ] 显示应用版本
- [ ] 显示 OMP 版本
- [ ] 恢复应用默认设置
- [ ] 恢复默认设置前进行确认

应用设置只保存以下内容：

```ts
type AppSettings = {
  ompExecutablePath?: string
  theme: "light" | "dark" | "system"
  selectedProviderId?: string
  selectedModelId?: string
}
```

应用设置中不保存完整 Provider、模型、角色或 API Key。

---

## 15. 页面状态和交互

### 功能清单

- [ ] 页面加载时显示 Skeleton
- [ ] 没有数据时显示空状态
- [ ] 加载失败时显示错误状态
- [ ] 所有保存操作显示加载状态
- [ ] 保存期间禁止重复提交
- [ ] 删除操作统一使用确认对话框
- [ ] 表单错误显示在对应输入框附近
- [ ] 保存按钮在内容没有变化时禁用
- [ ] 未保存修改需要有明显提示
- [ ] 离开未保存表单时进行确认
- [ ] Provider 和模型状态使用 Badge 显示
- [ ] 按钮和纯图标操作提供 Tooltip
- [ ] 表单和弹窗支持键盘操作
- [ ] 危险操作与普通操作有明显区别
- [ ] 窗口缩小时页面仍可以正常使用
- [ ] 页面切换可以使用简单动画
- [ ] 弹窗和状态变化可以使用简单动画
- [ ] 避免大面积、长时间或影响操作的动画
- [ ] 成功和失败消息统一使用 Sonner

Zustand 只管理页面选择、弹窗、当前选中项和临时界面状态。

表单内容由 React Hook Form 管理，表单规则由 Zod 管理。

---

## 16. 日志和错误处理

### 日志内容

第一阶段记录：

- 应用启动和退出
- OMP 检测结果
- 配置文件读取结果
- 配置文件写入结果
- 配置备份结果
- 模型测试结果
- 无敏感信息的错误详情

### 安全要求

- [ ] 不记录 API Key
- [ ] 不记录完整认证请求头
- [ ] 不记录完整模型响应
- [ ] 不记录用户本地文件内容
- [ ] 文件路径可以记录，但需要避免记录无关目录
- [ ] 前端错误和 Rust 错误使用统一格式
- [ ] 错误信息提供可读说明
- [ ] 需要时保留原始错误码用于排查

统一错误结构可以保持简单：

```ts
type AppError = {
  code: string
  message: string
  detail?: string
}
```

---

## 17. 工程基础要求

### 前端

- [ ] React 和 TypeScript 项目结构
- [ ] TypeScript Strict Mode
- [ ] Tailwind CSS
- [ ] shadcn/ui
- [ ] Zustand
- [ ] React Hook Form
- [ ] Zod
- [ ] `@hookform/resolvers`
- [ ] lucide-react
- [ ] Motion
- [ ] Sonner
- [ ] ESLint
- [ ] Prettier
- [ ] 统一的 Tauri 调用封装
- [ ] 统一的加载和错误处理

### Rust

- [ ] 统一的命令入口
- [ ] 统一的错误返回
- [ ] OMP 环境检测模块
- [ ] Provider 配置模块
- [ ] 模型配置模块
- [ ] 角色配置模块
- [ ] 模型测试模块
- [ ] 配置备份模块
- [ ] 应用设置模块
- [ ] 日志模块
- [ ] Rust fmt
- [ ] Clippy

### Tauri 安全

- [ ] 只开放当前功能需要的 Tauri 权限
- [ ] 前端不能执行任意 Shell 命令
- [ ] 前端不能读写任意文件
- [ ] OMP 命令只能由固定的 Rust 逻辑调用
- [ ] 文件选择只能用于选择 OMP 可执行文件
- [ ] 打开目录只能打开应用已经确认的配置目录
- [ ] 配置合理的 CSP
- [ ] API Key 不通过事件广播发送

---

## 18. 测试范围

### Rust 测试

- [ ] Provider 配置解析
- [ ] 模型配置解析
- [ ] 角色配置解析
- [ ] 未知 YAML 字段保留
- [ ] Provider 新增、修改和删除
- [ ] 模型新增、修改、复制和删除
- [ ] Provider ID 修改后的引用更新
- [ ] 模型 ID 修改后的引用更新
- [ ] 配置文件原子写入
- [ ] 配置文件备份
- [ ] 配置外部修改检测
- [ ] API Key 脱敏
- [ ] 三种协议的请求构造
- [ ] 三种协议的响应处理
- [ ] 网络错误分类
- [ ] 用户取消模型测试
- [ ] 请求超时处理

### 前端测试

- [ ] Provider 表单校验
- [ ] 模型表单校验
- [ ] API Key 留空时保留旧值
- [ ] Provider 新增和编辑流程
- [ ] Provider 删除确认
- [ ] 模型新增和编辑流程
- [ ] 模型复制流程
- [ ] 模型删除确认
- [ ] 角色模型选择
- [ ] 角色清除操作
- [ ] 未保存修改提示
- [ ] 模型测试状态
- [ ] 加载、空状态和错误状态
- [ ] Sonner 成功和失败提示

### 手工验收

- [ ] 使用一份脱敏的现有 OMP 配置进行读取测试
- [ ] 创建一个自定义 Provider
- [ ] 保存直接 API Key
- [ ] 创建多个模型
- [ ] 复制和删除模型
- [ ] 测试三种 API 协议
- [ ] 配置全部十个内置角色
- [ ] 修改 Provider ID 并检查角色引用
- [ ] 修改模型 ID 并检查角色引用
- [ ] 手动修改配置文件并验证冲突提示
- [ ] 模拟写入失败并检查原文件是否完整
- [ ] 检查日志中是否出现 API Key

---

## 19. MVP 验收标准

第一阶段完成后，应满足以下条件：

- [ ] 应用可以自动检测本机 OMP
- [ ] 用户可以手动设置 OMP 路径
- [ ] 应用可以读取已有 OMP 配置
- [ ] 用户可以新增、编辑、搜索和删除自定义 Provider
- [ ] 用户可以使用直接 API Key
- [ ] 用户可以创建、编辑、复制、搜索和删除模型
- [ ] 用户可以测试模型连接
- [ ] 模型测试支持三种约定协议
- [ ] 用户可以配置全部十个内置角色
- [ ] Provider 或模型改名后，角色引用能够正确更新
- [ ] 应用重启后可以继续读取已有配置
- [ ] 配置保存失败不会损坏原文件
- [ ] 配置被外部修改时不会被静默覆盖
- [ ] 每次写入前都会创建备份
- [ ] 应用不会把 API Key 保存到自身配置
- [ ] API Key 不会出现在前端状态和日志中
- [ ] 应用不提供启动 OMP 的功能
- [ ] 应用不提供任何终端相关功能

---

## 20. 建议开发顺序

### 第一步：项目基础

完成 Tauri、React、TypeScript、Tailwind CSS、shadcn/ui 和基础页面框架。

### 第二步：OMP 检测

完成 OMP 路径、版本和配置目录检测。

### 第三步：配置读取

完成 `models.yml` 和 `config.yml` 的读取与解析。

### 第四步：Provider 管理

完成 Provider 新增、编辑、删除和 API Key 管理。

### 第五步：模型管理

完成模型新增、编辑、复制、搜索和删除。

### 第六步：模型测试

完成三种协议的模型连接测试和错误分类。

### 第七步：角色管理

完成十个内置角色和 Thinking Level 配置。

### 第八步：安全写入

完成备份、临时文件写入、外部修改检测和失败保护。

### 第九步：设置和体验

完成主题、目录入口、错误提示、加载状态和未保存提醒。

### 第十步：测试和验收

补齐 Rust 测试、前端测试和手工验收。